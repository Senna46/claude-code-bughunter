# Claude Code BugHunter Configuration
# Copy this file to .env and fill in the values

# ============================================================
# Required: GitHub target configuration
# ============================================================

# GitHub owners to monitor (comma-separated, supports both users and organizations)
# Example: BUGHUNTER_GITHUB_ORGS=myuser,my-org
BUGHUNTER_GITHUB_ORGS=

# Specific repositories to monitor (comma-separated, owner/repo format)
# If set, only these repos are monitored (overrides org-wide scanning)
# Example: BUGHUNTER_GITHUB_REPOS=myorg/repo1,myorg/repo2
BUGHUNTER_GITHUB_REPOS=

# ============================================================
# Optional: Behavior configuration
# ============================================================

# Polling interval in seconds (default: 60)
BUGHUNTER_POLL_INTERVAL=60

# Bot name used in PR comments (default: bughunter)
# Users will type /<bot_name> push <sha> to approve fixes
BUGHUNTER_BOT_NAME=bughunter

# Autofix mode (default: branch)
#   off    - Bug detection only, no fix generation
#   branch - Create a fix branch, wait for approval via /bughunter push <sha>
#   commit - Commit fixes directly to the PR's head branch (auto-push)
#   pr     - Create a fix branch and open a new PR automatically
BUGHUNTER_AUTOFIX_MODE=branch

# Working directory for cloning repositories (default: ~/.bughunter/repos)
BUGHUNTER_WORK_DIR=

# Maximum diff size in characters to analyze (default: 100000)
BUGHUNTER_MAX_DIFF_SIZE=100000

# Claude model to use (optional, uses claude CLI default if not set)
# Example: claude-sonnet-4-5-20250929
BUGHUNTER_CLAUDE_MODEL=

# Log level: debug, info, warn, error (default: info)
BUGHUNTER_LOG_LEVEL=info

# SQLite database path (default: ~/.bughunter/state.db)
BUGHUNTER_DB_PATH=

# ============================================================
# Docker: Authentication tokens (macOS)
# ============================================================

# GitHub token (required for Docker on macOS)
# macOS stores gh CLI tokens in Keychain, not in files.
# Not required on Linux (gh CLI stores token in ~/.config/gh/hosts.yml).
# Get your token with: gh auth token
GH_TOKEN=

# Claude OAuth token (required for Docker on macOS)
# macOS stores Claude credentials in Keychain, not in files.
# Not required on Linux (credentials stored in ~/.claude/.credentials.json).
# Generate a long-lived token with: claude setup-token
# The token is valid for ~1 year.
CLAUDE_CODE_OAUTH_TOKEN=
